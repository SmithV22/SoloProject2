
import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    randomQuote: {
        textAlign: 'center',
        margin: '20px',
        display: 'flex',
        justifyContent: 'center',
    }
})) ;